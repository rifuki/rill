//! The local signer, distributed as a single binary.
//!
//! It holds the key, trusts no bytes from the server without independent inspection, and
//! signs only an envelope that has passed every state transition in `rill-policy`.

fn main() {
    println!("rill-wallet: scaffold");
}
